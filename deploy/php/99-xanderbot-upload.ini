; Install on server (adjust PHP version path):
;   sudo cp deploy/php/99-xanderbot-upload.ini /etc/php/8.2/fpm/conf.d/
;   sudo systemctl reload php8.2-fpm
; Verify: php -i | grep -E "upload_max_filesize|post_max_size"

upload_max_filesize = 5M
post_max_size = 8M
