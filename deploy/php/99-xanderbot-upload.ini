; Install on server:
;   cd /var/www/xanderbot && sudo bash deploy/php/install-upload-limits.sh
; Or manually copy into every conf.d found by: find /etc/php -type d -name conf.d
; Verify web SAPI (not only CLI): php -i | grep upload_max_filesize

upload_max_filesize = 5M
post_max_size = 8M
